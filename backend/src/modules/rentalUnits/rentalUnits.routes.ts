import { Router } from 'express';

/**
 * Rental unit routes — mounted at `/v1/rental-units` by src/routes/v1.ts.
 *
 * Stub created in Wave 1 and already wired up, so T2.1 can implement the five endpoints
 * of §3.6 by editing this file alone. Do not rename the export: `v1.ts` imports it by
 * name and is not owned by that task.
 */
export const rentalUnitsRouter = Router();
