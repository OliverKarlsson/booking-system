import { Router } from 'express';

/**
 * Dashboard routes — mounted at `/v1/dashboard` by src/routes/v1.ts, so the endpoint
 * itself is `router.get('/', …)` here.
 *
 * Stub created in Wave 1 and already wired up, so T2.3 can implement §3.6/§3.7 by
 * editing this file alone. Do not rename the export: `v1.ts` imports it by name and is
 * not owned by that task.
 */
export const dashboardRouter = Router();
